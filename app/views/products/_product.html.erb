<p> <%= link_to product.name, product_path(product)  %> <span> $ <%= product.price %> </span>
<%= link_to "Edit", edit_product_path(product) if params[:action] == "show"%>
<%= link_to "Delete", product_path(product), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} if params[:action] == "show" %>
